---
layout: layouts/home.njk
eleventyNavigation:
  key: Work
  order: 3
---
<h1>All Work</h1>

<div class="featured-works">
  {% for post in collections.work %}
    <div class="work-post">
      <img src="{{ post.data.image }}" alt="{{ post.data.title }}">
      <p><span class="hover-text">{{ post.data.title }}</span></p> <!-- Overlay description on image -->
    </div>
  {% endfor %}
</div>
